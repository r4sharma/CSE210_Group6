name: CI/CD

on:
  push:
    branches:
      - main
      - dev
  pull_request:
    branches: 
      - main
      - dev

jobs:
  jest:
    runs-on: ubuntu-latest
    needs: [dependencies]
    steps:
      - uses: actions/checkout@v1

      - name: Run Tests
        run: |
          npm ci 
          npm test
      
      - name: Run Lint Check
        run: npm run lint

  build:
    runs-on: ubuntu-latest
    needs: [jest]
    steps:
      - uses: actions/checkout@v1

      - name: Run Build
        run: npm run build

  jsdoc:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/checkout@v1

      - name: Run JSDoc
        run: npm run doc
