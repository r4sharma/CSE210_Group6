/**
 * Sample function for testing CICD pipeline
 * @param {int} a First Number
 * @param {int} b Second Number
 * @return {int} Difference
 */
function subtraction(a, b) {
  return (a - b);
}
module.exports = subtraction;
